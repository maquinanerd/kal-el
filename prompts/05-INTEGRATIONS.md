# Frontend + Automation Integration Prompt

Implement Phases 8–9: typed REST SDK, authenticated preview, reference frontend fixture for cached delivery/revalidation, service credentials, MN26/MNScr-compatible create/update/publish, provenance/external keys and retry/idempotency tests.

Do not build a full portal design.
