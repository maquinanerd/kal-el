{
  "name": "@kal-el/fixture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:e2e": "vitest run"
  },
  "dependencies": {
    "@kal-el/contracts": "workspace:*",
    "@kal-el/events": "workspace:*",
    "@kal-el/sdk": "workspace:*",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@kal-el/api": "workspace:*",
    "@kal-el/db": "workspace:*",
    "@kal-el/testkit": "workspace:*",
    "@kal-el/worker": "workspace:*",
    "@types/pg": "^8.11.10",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
